---
import Layout from "@/layouts/Layout.astro";
import Breadcrumbs from "@/components/Breadcrumbs.astro";
import { TreatmentPlan } from "@/components/TreatmentPlan";
import { PLAN_DAYS } from "@/data/content";
import { breadcrumbSchema, howToSchema, schemaGraph } from "@/lib/seo";

const PATH = "/plan";
const TITLE = "7-Day Fungus Gnat Starter Plan";
const DESCRIPTION = "Your first-week checklist for fungus gnats: manage moisture, trap adults, and treat larvae if needed. Heavier infestations may require continued monitoring and treatment beyond day 7.";

const schema = schemaGraph(
  breadcrumbSchema([{ name: "7-Day Starter Plan", path: PATH }]),
  howToSchema({
    name: TITLE,
    description: DESCRIPTION,
    path: PATH,
    totalTime: "P7D",
    steps: PLAN_DAYS.map((day) => ({
      name: `Day ${day.day}: ${day.title}`,
      text: day.tasks.join(" "),
    })),
  }),
);
---

<Layout title={`${TITLE} | Stop Fungus Gnats`} description={DESCRIPTION} schema={[schema]}>
  <Breadcrumbs items={[{ name: "7-Day Starter Plan", path: PATH }]} />
  <main id="main-content" class="mx-auto max-w-3xl px-4 py-12 sm:px-6" data-reveal>
    <span class="label-mono inline-flex -rotate-2 items-center rounded-md border-2 border-ink bg-primary px-3 py-1.5 text-[11px] text-primary-foreground shadow-stamp-sm">
      The protocol
    </span>
    <h1 class="mt-4 font-display text-4xl font-semibold tracking-tight sm:text-5xl">
      7-Day Fungus Gnat <span class="text-primary italic">Starter Plan</span>
    </h1>
    <p class="mt-4 max-w-xl leading-relaxed text-muted">
      Your first-week checklist. Heavier infestations may require continued monitoring and
      treatment beyond day 7 — that's normal, not a sign it isn't working. Check off each task as
      you go; your progress stays on this device. Keep monitoring after adult numbers fall, and
      continue moisture management and any labeled treatment long enough to account for remaining
      immature stages — that's the step people skip, and a common reason gnats seem to come back.
    </p>
    <div class="mt-10">
      <TreatmentPlan client:idle />
    </div>
    <section class="mt-12 glass rounded-xl p-6 shadow-stamp-sm sm:p-7" data-reveal>
      <p class="label-mono text-[11px] text-primary">Still seeing adults after day 7?</p>
      <h2 class="mt-2 font-display text-xl font-semibold tracking-tight">Keep going into weeks 2–4</h2>
      <p class="mt-2 text-sm leading-relaxed text-muted">
        That's expected for a heavier infestation, not a failed plan. If you're using a Bti
        product, keep following its label on reapplication; keep managing moisture for each plant
        appropriately, and replace sticky cards as they fill. Stop counting from zero and start
        watching the trend on your traps — a falling count week over week means it's working, even
        before it hits zero. See
        <a href="/how-to-know-treatment-is-working" class="text-primary hover:underline">how to read that trend</a> in detail.
      </p>
      <a
        href="/fungus-gnat-troubleshooting"
        class="focus-ring group mt-4 inline-flex min-h-11 items-center gap-2 rounded-md text-sm font-semibold text-primary"
      >
        Troubleshoot a stalled infestation
      </a>
    </section>
  </main>
</Layout>
